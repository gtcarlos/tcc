<%= form_for(@investor_group) do |f| %>
  <% if @investor_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@investor_group.errors.count, "error") %> prohibited this investor_group from being saved:</h2>

      <ul>
      <% @investor_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :area %><br />
    <%= f.number_field :area %>
  </div>
  <div class="field">
    <%= f.label :privacy %><br />
    <%= f.number_field :privacy %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
