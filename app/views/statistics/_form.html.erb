<%= form_for(@statistic) do |f| %>
  <% if @statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statistic.errors.count, "error") %> prohibited this statistic from being saved:</h2>

      <ul>
      <% @statistic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hits %><br />
    <%= f.number_field :hits %>
  </div>
  <div class="field">
    <%= f.label :published_posts %><br />
    <%= f.number_field :published_posts %>
  </div>
  <div class="field">
    <%= f.label :post_views %><br />
    <%= f.number_field :post_views %>
  </div>
  <div class="field">
    <%= f.label :post_shared %><br />
    <%= f.number_field :post_shared %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
