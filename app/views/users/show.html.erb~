<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>
<p>
  <b>Email:</b>
  <%= @user.email %>
</p>
<p>
  <b>Role</b>
  <%= @user.roles_mask %>
</p>
<p>
<% unless current_user.friends.include?(@user) %>
  <%= form_for :friend, :url => friendship_index_path do |f| %>
    <%= f.hidden_field :friend_id, value: @user.id %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <button class="btn btn-info btn-small btn-seguir">Seguir</button>
  <% end %>
<% else %>
  <%= button_to 'Deixar de seguir', friendship_path, method: :delete, class: 'btn btn-small btn-danger', confirm: 'Voce tem certeza?' %>
<% end %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
