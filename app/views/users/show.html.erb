<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>
<p>
  <b>Email:</b>
  <%= @user.email %>
</p>
<p>
  <b>Role</b>
  <%= @user.roles_mask %>
</p>
<p>
<% unless current_user.friends.include?(@user) %>
  <%= form_for :friend, :url => friendship_path do |f| %>
    <%# f.hidden_field :friend_id, value user.id %>
    <button class="btn btn-info btn-small">Seguir</button>
  <% end %>
<% else %>
  <span>Ja esta seguindo</span>
<% end %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
