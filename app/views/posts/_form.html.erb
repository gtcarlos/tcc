<div class="status">
<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="status session">
  <div class="field">
    <%= f.text_field :title, :id => "titulo", :placeholder => "Titulo da publicacao" %>
  </div>
  <div class="field">
    <%= f.text_area :content, :id => "conteudo", :placeholder => "O que voce esta pensando?" %>
  </div>
  <div class="actions">
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= f.submit "Publicar", :class => "btn", :id => "publicar" %>
  </div>
<% end %>
  </div>
 </div>
